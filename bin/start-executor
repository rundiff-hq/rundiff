#!/usr/bin/env bash
set -Eeuo pipefail

# Provider-neutral executor-service process entrypoint.
# The platform supplies DATABASE_URL, PORT and the executor service environment.
bin/rails db:prepare

exec bin/rails server -b 0.0.0.0 -p "${PORT:-3000}"
