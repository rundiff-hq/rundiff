# Copy to .env on each production host.
# Keep all three references immutable for deterministic rollback.
RUNDIFF_IMAGE_TAG=sha-<full-git-sha>
RUNDIFF_CLOUDFLARED_IMAGE=cloudflare/cloudflared@sha256:<digest>
# Executor host only:
RUNDIFF_POSTGRES_IMAGE=postgres@sha256:<digest>
